name: Build MyAPI Docker image (manual branch)

env:
  IMAGE_REPOSITORY: ghcr.io/forcemind/myapi

on:
  workflow_dispatch:
    inputs:
      branch:
        description: "Approved branch to build"
        required: true
        type: choice
        options:
          - main
          - alpha
          - nightly
      confirm:
        description: "Type PUBLISH only after maintainer approval and GHCR review"
        required: true
        type: string

concurrency:
  group: myapi-ghcr-branch-${{ inputs.branch }}
  cancel-in-progress: false

jobs:
  prepare:
    name: Prepare Docker tags
    runs-on: ubuntu-latest
    outputs:
      branch: ${{ steps.version.outputs.branch }}
      sha: ${{ steps.version.outputs.sha }}
      tag_prefix: ${{ steps.version.outputs.tag_prefix }}
      version: ${{ steps.version.outputs.version }}
    permissions:
      contents: read
    steps:
      - name: Validate branch allowlist
        env:
          BRANCH_NAME: ${{ inputs.branch }}
        run: |
          case "$BRANCH_NAME" in
            main|alpha|nightly)
              ;;
            *)
              echo "::error::branch must be one of the approved release branches: main, alpha, nightly"
              exit 1
              ;;
          esac

      - name: Check out branch
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          fetch-depth: 1
          ref: refs/heads/${{ inputs.branch }}
          persist-credentials: false

      - name: Resolve Docker tags
        id: version
        env:
          BRANCH_NAME: ${{ inputs.branch }}
        run: |
          TAG_PREFIX=$(printf '%s' "$BRANCH_NAME" | tr '[:upper:]' '[:lower:]' | sed -E 's/[^a-z0-9_.-]+/-/g; s/^[.-]+//; s/[.-]+$//')
          TAG_PREFIX=${TAG_PREFIX:0:105}
          TAG_PREFIX=$(printf '%s' "$TAG_PREFIX" | sed -E 's/[.-]+$//')
          if [ -z "$TAG_PREFIX" ]; then
            echo "::error::Branch '$BRANCH_NAME' cannot be converted to a valid Docker tag prefix"
            exit 1
          fi

          SHA=$(git rev-parse HEAD)
          SHORT_SHA=$(git rev-parse --short HEAD)
          VERSION="${TAG_PREFIX}-$(date +'%Y%m%d')-${SHORT_SHA}"

          echo "branch=$BRANCH_NAME" >> "$GITHUB_OUTPUT"
          echo "sha=$SHA" >> "$GITHUB_OUTPUT"
          echo "tag_prefix=$TAG_PREFIX" >> "$GITHUB_OUTPUT"
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
          echo "Prepared Docker tags for $BRANCH_NAME at $SHORT_SHA"

  build_single_arch:
    name: Build & push (${{ matrix.arch }}) [native]
    needs: [prepare]
    if: ${{ inputs.confirm == 'PUBLISH' && vars.MYAPI_ENABLE_DOCKER_PUBLISH == 'true' }}
    environment: docker-publish
    strategy:
      fail-fast: false
      matrix:
        include:
          - arch: amd64
            platform: linux/amd64
            runner: ubuntu-latest
          - arch: arm64
            platform: linux/arm64
            runner: ubuntu-24.04-arm
    runs-on: ${{ matrix.runner }}
    permissions:
      contents: read
      packages: write
      id-token: write
    steps:
      - name: Check out branch
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          fetch-depth: 1
          ref: ${{ needs.prepare.outputs.sha }}
          persist-credentials: false

      - name: Write VERSION
        id: validate
        run: |
          if [[ "${{ vars.MYAPI_ENABLE_DOCKER_PUBLISH }}" != "true" || "${{ inputs.confirm }}" != "PUBLISH" ]]; then
            echo "::error::Docker publishing is disabled or not confirmed"
            exit 1
          fi
          echo "${{ needs.prepare.outputs.version }}" > VERSION
          echo "IMAGE_REPOSITORY=${IMAGE_REPOSITORY}" >> "$GITHUB_ENV"
          echo "image_repository=${IMAGE_REPOSITORY}" >> "$GITHUB_OUTPUT"
          echo "Publishing version: ${{ needs.prepare.outputs.version }} for ${{ matrix.arch }}"

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@bb05f3f5519dd87d3ba754cc423b652a5edd6d2c # v4.2.0

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@c99871dec2022cc055c062a10cc1a1310835ceb4 # v4.3.0
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata (labels)
        id: meta
        uses: docker/metadata-action@dc802804100637a589fabce1cb79ff13a1411302 # v6.2.0
        with:
          images: ${{ steps.validate.outputs.image_repository }}

      - name: Build & push single-arch
        id: build
        uses: docker/build-push-action@53b7df96c91f9c12dcc8a07bcb9ccacbed38856a # v7.3.0
        with:
          context: .
          platforms: ${{ matrix.platform }}
          push: true
          tags: |
            ${{ steps.validate.outputs.image_repository }}:${{ needs.prepare.outputs.tag_prefix }}-${{ matrix.arch }}
            ${{ steps.validate.outputs.image_repository }}:${{ needs.prepare.outputs.version }}-${{ matrix.arch }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          provenance: mode=max
          sbom: true

      - name: Install cosign
        uses: sigstore/cosign-installer@6f9f17788090df1f26f669e9d70d6ae9567deba6 # v4.1.2

      - name: Sign image with cosign
        run: cosign sign --yes "${IMAGE_REPOSITORY}@${{ steps.build.outputs.digest }}"

      - name: Output digest
        run: |
          echo "### Docker Image Digest (${{ matrix.arch }})" >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
          echo "${IMAGE_REPOSITORY}:${{ needs.prepare.outputs.tag_prefix }}-${{ matrix.arch }}" >> $GITHUB_STEP_SUMMARY
          echo "${IMAGE_REPOSITORY}:${{ needs.prepare.outputs.version }}-${{ matrix.arch }}" >> $GITHUB_STEP_SUMMARY
          echo "${{ steps.build.outputs.digest }}" >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY

  create_manifests:
    name: Create multi-arch manifests (approved registry)
    needs: [prepare, build_single_arch]
    if: ${{ inputs.confirm == 'PUBLISH' && vars.MYAPI_ENABLE_DOCKER_PUBLISH == 'true' }}
    environment: docker-publish
    runs-on: ubuntu-latest
    permissions:
      packages: write
      id-token: write
    steps:
      - name: Verify publish gate and repository
        run: |
          if [[ "${{ vars.MYAPI_ENABLE_DOCKER_PUBLISH }}" != "true" || "${{ inputs.confirm }}" != "PUBLISH" ]]; then
            echo "::error::Docker publishing is disabled or not confirmed"
            exit 1
          fi
          echo "IMAGE_REPOSITORY=${IMAGE_REPOSITORY}" >> "$GITHUB_ENV"

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@c99871dec2022cc055c062a10cc1a1310835ceb4 # v4.3.0
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Create & push manifest (GHCR - branch)
        run: |
          docker buildx imagetools create \
            -t "${IMAGE_REPOSITORY}:${{ needs.prepare.outputs.tag_prefix }}" \
            "${IMAGE_REPOSITORY}:${{ needs.prepare.outputs.tag_prefix }}-amd64" \
            "${IMAGE_REPOSITORY}:${{ needs.prepare.outputs.tag_prefix }}-arm64"

      - name: Create & push manifest (GHCR - versioned)
        run: |
          docker buildx imagetools create \
            -t "${IMAGE_REPOSITORY}:${{ needs.prepare.outputs.version }}" \
            "${IMAGE_REPOSITORY}:${{ needs.prepare.outputs.version }}-amd64" \
            "${IMAGE_REPOSITORY}:${{ needs.prepare.outputs.version }}-arm64"

      - name: Install cosign
        uses: sigstore/cosign-installer@6f9f17788090df1f26f669e9d70d6ae9567deba6 # v4.1.2

      - name: Sign manifests with cosign
        run: |
          cosign sign --yes "${IMAGE_REPOSITORY}:${{ needs.prepare.outputs.tag_prefix }}"
          cosign sign --yes "${IMAGE_REPOSITORY}:${{ needs.prepare.outputs.version }}"

      - name: Output manifest digest
        run: |
          echo "### Multi-arch Manifest Digests" >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
          docker buildx imagetools inspect "${IMAGE_REPOSITORY}:${{ needs.prepare.outputs.tag_prefix }}" >> $GITHUB_STEP_SUMMARY
          echo "---" >> $GITHUB_STEP_SUMMARY
          docker buildx imagetools inspect "${IMAGE_REPOSITORY}:${{ needs.prepare.outputs.version }}" >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
